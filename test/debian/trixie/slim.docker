FROM debian:trixie-slim

# ghcr.io/suhlig/dotfiles-debian-trixie-slim

RUN apt-get update \
 && apt-get install -y --no-install-recommends \
      git \
      stow \
      zsh \
 && rm -rf /var/lib/apt/lists/*

CMD ["zsh"]
