# select account; then print and copy code to clipboard
# arguments get passed to fzf as query
function totp {
  totp-keychain ls | tr -d - | fzf-tmux --query "$@" | xargs -I ACCT totp-keychain get ACCT | tee /dev/stderr | pbcopy
}
